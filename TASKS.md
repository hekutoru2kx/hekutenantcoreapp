# Tasks

Single source of truth for this project's tasks. Structure is shared across the whole
family (ludemia, hekucoreapp, hekutenantcoreapp, gestamind): **Pending** = queued work,
**Deferred** = intentionally postponed with a stated trigger, **Done** = shipped.

## Pending


## Deferred — intentional, revisit on trigger

_Nothing deferred._

## Done

- [x] **Fixed the 12 failing frontend unit tests: default scaffold specs had no providers (ported from hekucoreapp/gestamind, 2026-09-24)** — the same 12 failures inherited from the shared scaffold: 10 "should create" specs with no providers (`NG0201` on Transloco / `ActivatedRoute`), plus `app.spec.ts`'s stale `"Hello, frontend"` assertion. Added `frontend/src/app/testing/test-providers.ts` (`provideTestApp()`), **byte-identical to gestamind's and hekucoreapp's**, and `providers: provideTestApp()` in the 10 component specs. `app.spec.ts` was replaced with gestamind's version verbatim (this repo's `app.ts`/`app.html` match gestamind's: a `routerLink` change-password anchor, tenant fields on `CurrentUser`, and the tenant-picker redirect backstop). It checks that the nav bar and outlet render and that the password-warning banner and its link toggle on `mustChangePassword`. **Verified:** `npx ng test --watch=false` shows **34 passed (19 files)**, fully green. `ng build` is clean (the bundle-budget warning was already there). These remain shallow "can be created" checks that prove DI wiring, not behavior.
- [x] **Fixed: `ContentService.UploadToSlotAsync` leaked a raw ImageSharp exception on a wrong-file-type upload (ported from hekucoreapp, 2026-09-24)** — this is the shared `ContentService` from the content-standardization port. It sniffed the format with `Image.DetectFormatAsync` and expected `null` for unrecognized data, but ImageSharp 3.x throws `UnknownImageFormatException`, so a non-image upload returned "Image cannot be loaded. Available decoders: ..." as the 400 body instead of the localized `ContentFileTypeNotAllowed`. The bug was first found in ludemia (2026-09-12, fixed there only in `CourseContentRepository.UploadFileAsync`) and tracked only in hekucoreapp's TASKS.md, although every family copy had it. The fix is byte-identical to hekucoreapp's: `Image.LoadAsync` inside `try/catch (ImageFormatException)` mapped to `ContentFileTypeNotAllowed`, with the format read from `image.Metadata.DecodedImageFormat`. New `Tests/Application/ContentServiceUploadTests.cs` (4 tests: plain text, PDF, a disallowed-but-valid PNG, and the happy path). **Verified:** `dotnet test` green (70/70).
- [x] **Logging & debugging infrastructure: Serilog, categories, EF interceptor, business-CRUD logging, SuperAdmin admin UI (2026-09-23)** — ported directly from gestamind (`bd96f22`), not from hekucoreapp: unlike the single-tenant cores, this repo shares gestamind's actual shape (multitenant, `ITenantScoped`, an existing `globalOnlyModules` SuperAdmin-exclusive mechanism, per-entity `IEntityTypeConfiguration` classes, and an already-identical `api/system/platform/...` route family), so gestamind's implementation is the close-fitting template here, re-scoped to this repo's own 12-controller/11-`AuditableEntity` list rather than structurally translated. Same design: two independent axes — severity (`LogLevel`: Trace..Critical/None, dependency-free in Domain, mirrors `Microsoft.Extensions.Logging.LogLevel`'s values so Infrastructure/Api convert with a plain cast) × category (`LogCategory`: Http/Database/Security/Integration/Business/System) — gated once in `CategoryLevelSwitches`/`CategoryLogger` (the sole `ICategoryLogger` entry point) before anything reaches Serilog. Three sinks, always all three: console + rolling file (`Logs/hekutenantcoreapp-.log`, the mandatory fallback) + a hand-rolled batched `SystemLogPostgresSink` writing to a new `system_logs` table via its own `NpgsqlConnection` — deliberately never through `HekutenantcoreappDbContext`/EF, so a DB-pool outage can't also kill logging. Password/secret safety, layered: `HttpRequestLoggingMiddleware` logs method/path/status/duration only, never request/response bodies; a global `SensitiveDataDestructuringPolicy` masks any `Password`/`Secret`/`Token`/`Key`-named property on any destructured type; `QueryLoggingInterceptor` (the sole producer of Database-category content — EF's own `.LogTo()` stays unwired) excludes `PasswordHash`/`SecurityStamp`/`ConcurrencyStamp` always and only includes parameter values in Development. Business-CRUD logging lives in `HekutenantcoreappDbContext.SaveChangesAsync` (now async): snapshots every `AuditableEntity` entry before the save (State must be read pre-save; the entries stay valid to read after, which is when an Added row's real id first exists), logs one line per entity via `BusinessLogPlanner` (pure, unit-tested) after a successful save. New `IAggregateItem` marker interface ported as pure infrastructure — **zero entities implement it**: none of this repo's 11 `AuditableEntity`-derived entities (`AppSettings`, `ContentItem`, `DeletedAccount`, `Employee`, `LocalizedText`, `MultiTenantSettings`, `Person`, `StoredFile`, `Tenant`, `TenantMembership`, `UserTenantRole`) is a genuine single-hop item-of-a-root the way gestamind's `DiagnosticTestItem`/`Observation`/`HistoryFieldOption` are — no repeatable child-item collection exists in this list, so forcing an implementer would have been wrong; the interface stays ready for the next one that qualifies. All ~44 catch blocks across the 10 controllers that had them (`AppSettingsController`, `AuthController`(×7), `ContentController`, `EmployeeController`(×7), `MultiTenantSettingsController`, `PersonController`(×8), `RoleManagementController`(×3), `TenantController`(×3), `UserController`(×4), `UserManagementController`(×9)) gained one `_categoryLogger.LogError(LogCategory.Http, ...)` line before their existing, unchanged `return` — `GeographyController`/`TenantRoleController` have no catch blocks and were skipped, same rule as both other ports. New SuperAdmin-exclusive `LoggingSettings` (6 fixed category rows + a retention singleton, `logging_category_settings`/`logging_retention_settings`, same recipe as `MultiTenantSettings`) with `LoggingSettingsRefreshHostedService` polling every 30s to push saved levels into the live switches (no restart) and running the daily `system_logs` retention sweep off the same timer; Security's minimum level is clamped server-side to never exceed Warning (it's the only audit trail for auth events). `LoggingSettingsPermission` added to `Program.cs`'s existing `globalOnlyModules` array alongside `TenantsPermission`/`RolesPermission`/`UserManagementPermission`/`MultiTenantSettingsPermission`, keeping logging platform-level rather than tenant-Admin-grantable. **Tenant-filterable log viewer kept faithful to gestamind, not simplified** (the one deliberate difference from the hekucoreapp/ludemia single-tenant ports, which stripped tenant-filtering entirely): `system_logs` carries a nullable `tenant_id` column (raw-SQL `CreateSystemLogsTable` migration, deliberately outside the EF model like gestamind's — written/read via raw Npgsql on both ends), and `SystemLogsQuery`/`SystemLogEntryDto`/`SystemLogsPageResult`/`SystemLogsRepository`/the Angular page all carry a `tenantId` filter through, unrestricted by tenant query filters since `SystemLogsController` is itself SuperAdmin-gated (`LoggingSettingsPermission.Read`) — ported gestamind's filter shape exactly, including its plain numeric Tenant ID input box (gestamind's own page has no tenant-name dropdown either, just a number field the SuperAdmin already knows how to use — not a simplification, matches upstream). Also checked, per the hekucoreapp port's own finding: this repo's `Program.cs` had the identical live bug hekucoreapp's port caught and gestamind's port fixed — the bootstrap admin's **generated** password was being logged via `app.Logger.LogWarning` (`"...Temporary password: {Password}..."`), leaking it into the new Serilog pipeline/sinks including the durable `system_logs` table. Fixed the same way: a generated password now prints via plain `Console.WriteLine` (never reaches `ICategoryLogger`/Serilog), one time, with a comment explaining why; the *configured*-password branch (which never echoes the value) still logs normally. Ported the 3 gestamind unit-test files into the existing `Hekutenantcoreapp.Tests` project (`Infrastructure/Logging/`): `CategoryLevelSwitchesTests` and `SensitiveDataDestructuringPolicyTests` ported verbatim; `BusinessLogPlannerTests` adapted off gestamind's `DiagnosticTest`/`Observation` entities (which don't exist here) onto this repo's own `Tenant` entity for the created/updated/deleted-line cases, plus a small in-test `FakeAggregateItem` double (paired with an unrelated tracked `Tenant` entry, since `BusinessLogPlanner` never reads `EntrySnapshot.Entry` on the item path) to exercise the roll-up-line behavior despite this repo having no real `IAggregateItem` entity to test against; gestamind's `Observation`-specific roll-up test was dropped as inapplicable. Also updated 8 pre-existing repository test files whose `HekutenantcoreappDbContext` constructor calls needed the new `ICategoryLogger` parameter (`NullCategoryLogger.Instance`). Two new migrations (`AddLoggingSettings`, `CreateSystemLogsTable`) applied cleanly to the live dev DB with no pending model changes afterward. Package additions: `Serilog`/`Serilog.AspNetCore`/`Serilog.Sinks.File` — `Microsoft.Extensions.Hosting.Abstractions` was already referenced, so (unlike hekucoreapp's port) no package-downgrade build fix was needed. Frontend: `logging-settings`/`system-logs` Angular pages (ported near-verbatim from gestamind) under `System > Platform` nav (matching the existing `MultiTenantSettings` placement), `logging-settings-management`/`system-logs-management` services, routes, i18n en+es. **Verified:** `dotnet build` clean; `dotnet test` **66/66** (54 baseline + 12 new, up from gestamind's port pattern); both migrations created via `dotnet ef migrations add` and applied to the live Supabase dev DB (`has-pending-model-changes` reports none afterward); `ng build` clean (pre-existing bundle-budget warning unchanged); a full live boot against the real dev DB confirmed all three sinks fire end-to-end — a throwaway verification script queried `system_logs` directly after a live `curl` smoke-test and found real rows landing with correct category/level/message (e.g. `Http/Information: GET /api/auth/registration-tenant-info responded 200 in 882ms`), and `logging_category_settings` seeded with the 6 expected default rows; API process confirmed stopped afterward, no process left running. Not browser-verified (Angular pages reasoned-through against gestamind's already browser-verified originals, not clicked through here).
- [x] **Generic content/attachments layer + Person profile picture (2026-09-11)** — ported from the hekucoreapp core (primary write-up + design rationale in `hekucoreapp/TASKS.md` and memory `project_content_standardization`). Same `ContentItem`/`StoredFile` polymorphic layer, `IContentStorage` (LocalDisk/AzureBlob), `ContentService` (validate/resize/store), and self-service `Person` profile-picture wiring on `UserController` — but `ContentItem`/`StoredFile` implement this repo's `ITenantScoped` marker interface instead of hekucoreapp's nullable-`tenant_id`-with-manual-derivation design, which gets tenant query-filtering, indexing, and insert-time stamping entirely for free from the existing `HekutenantcoreappDbContext` machinery; `ContentService`/`ContentRepository` port **byte-identical** (only the namespace differs) because of it. Per-entity `IEntityTypeConfiguration` classes (`ContentItemConfiguration`/`StoredFileConfiguration`) instead of hekucoreapp's inline `OnModelCreating`, matching this repo's existing convention. New `Tenant.StoragePrefix` (opaque 12-char slug, not the tenant's int id) partitions blob storage per tenant — auto-generated for every new `Tenant` row centrally in `HekutenantcoreappDbContext.SaveChangesAsync` (covers admin-created tenants, the startup default tenant, and tests with no per-call-site wiring), resolved at upload time by a new `TenantContentPartitionResolver` reading the same `CurrentTenantId` the tenant query filter already uses. This also drove a same-day simplification back-ported into hekucoreapp: `IContentStorage.SaveAsync` no longer takes a caller-supplied partition — the Application layer has no clean way to resolve "the current tenant" itself, so each storage implementation resolves its own via an internal `IContentPartitionResolver` (a stub in the single-tenant cores, this repo's tenant-aware one here). Same `AppSettings.Content*` admin-tunable limits fields as hekucoreapp, with the identical `HasDefaultValue` fix applied from the start (the hekucoreapp port caught, the hard way, that a plain `AddColumn` on an existing singleton row backfills to CLR zeros — an empty allow-list — without it). Migration also had to hand-edit the `Tenant.StoragePrefix` `AddColumn`: EF's generated single shared default would collide under the new unique index the moment there's more than one existing tenant, so it's added nullable, backfilled per-row with a random slug via `migrationBuilder.Sql`, then `AlterColumn`'d to NOT NULL. **Verified:** `dotnet build` clean; `dotnet test` 54/54 (no regressions); migration applied to the live dev DB (backfill + unique index succeeded); app boots clean against it (`ValidateOnBuild` confirms the DI graph, including the new `TenantContentPartitionResolver`); `ng build` clean (pre-existing bundle-budget warning unchanged); `ng test` shows the same pre-existing missing-provider smoke-test failures as hekucoreapp, no new ones. Not browser-verified. Admin-side (non-self-service) profile-picture management and gestamind's port are follow-up work.
- [x] **CSV reference-data seeder + geography seed data (2026-09-10)** — ported from the hekucoreapp core (primary write-up in `hekucoreapp/TASKS.md`). This DB's `countries/states/cities` were **empty** (lost in the 2026-09-09 recreation and never re-imported). New generic `CsvSeeder.SeedAsync(DbContext, folder, params (table, columns)[])` in `Infrastructure/Data` — for each named table currently empty, decompresses `Resources/<folder>/<table>.csv.gz` and bulk-loads it via PostgreSQL `COPY` (header skipped, body streamed raw, identity sequence `setval`'d past the seeded ids), no-op once populated. `GeographySeeder.SeedAsync(db)` is the thin caller (countries → states → cities), invoked in `Program.cs` startup right after `AppSettingsSeeder`. Resources `backend/Hekutenantcoreapp.Infrastructure/Resources/Geography/{countries,states,cities}.csv.gz` (byte-identical to the other three repos — md5 `59d175d5…`; ~1.4 MB total), shipped via the csproj `<None … CopyToOutputDirectory>`. Reusable for future project-DB reference data. **Verified:** `dotnet build` clean, `dotnet test` 54/54, and a real first boot against the live DB **loaded 250 / 5,308 / 152,967** into the empty tables; a second boot no-ops.
- [x] **Auto-create the bootstrap admin on a fresh/empty database (2026-09-10)** — ported from the hekucoreapp core (primary write-up in `hekucoreapp/TASKS.md`). A fresh DB used to need the admin to self-register through the app and then an app restart to be promoted; the `Program.cs` startup block only ever granted roles to an already-existing user, never created one. Now, when `BootstrapAdminEmail` is set **and `AspNetUsers` is entirely empty** (`!await userManager.Users.AnyAsync()`), the startup scope creates the user (`EmailConfirmed = true`, `MustChangePassword = true`, `IsActive = true`, `UserName = email`) **before** the existing `bootstrapUser` lookup — so the same pass then wires `SuperAdmin` + default-tenant membership + tenant-scoped `Admin` with no restart. Password from a new optional `BootstrapAdminPassword` key (blank default in `appsettings.json`), else a random 20-char CSPRNG password logged once at `Warning`; `CreateAsync` failure logs at `Warning` and falls through. No migration (Identity data). **Verified:** `dotnet build` clean (pre-existing warnings unchanged), `dotnet test` 54/54 green, API boots cleanly against the live DB — the `AnyAsync` guard runs and skips the create block (DB already has users); empty-DB happy path reasoned-through, not browser-verified.
- [x] **Ported `DataTableController<T>` from the hekucoreapp core (2026-09-09)** — the reusable admin-table state class (built in hekucoreapp the same day; server + client data modes, owns column order/visibility/sort/pagination + `localStorage` prefs + the `<app-column-reorder>` and CSV-export projections, page keeps its own `<table mat-table>` markup). Copied `frontend/src/app/shared/data-table-controller.ts` **verbatim** (+ `data-table-controller.spec.ts`, 12 pure-class tests — only the `STORAGE_PREFIX` in `column-preferences.ts` differs per repo and the controller routes through that helper). Migrated all 5 admin tables: **user / person / tenant / employee management** (server mode, `persistSort: true` to keep their existing sort-persistence behaviour) and **role-management** (converted to **client** mode — `MatTableDataSource` + `MatSort` + `MatPaginator`, `name`/`claims` now sortable). `tenant-role-management` and `employee-role-assignment` left alone (per-row editors, not data grids). Each page dropped ~90–110 lines of copy-pasted glue. **Verified:** `ng build` clean (`strictTemplates` is on here and caught every rebinding gap during the edit); pre-existing bundle-budget warning unchanged (+50 bytes); `ng test` — the 12 new controller tests pass, no regressions (the pre-existing component smoke-test failures are missing-provider gaps on `main` too). Not browser-verified. Follow-up ports: gestamind, ludemia — tracked in `hekucoreapp/TASKS.md`.
- [x] **Untracked `wwwroot`; Azure deploy workflow now builds the Angular frontend (2026-09-09)** — an Azure-generated deploy workflow (`main_hekutenantcoreapp.yml`) was added this day; it built/published only `./backend` with no frontend step and a pathless `dotnet build` that wouldn't have found `backend/Hekutenantcoreapp.slnx`. With `wwwroot/` now `.gitignored` (kept via `.gitkeep`, 8 tracked bundle files `git rm --cached`'d) it would have deployed an empty `wwwroot`. Fixed the workflow: Node setup + `npm ci` + `npm run build` + "Copy Angular build to wwwroot" + `dotnet build ./backend` / `dotnet test ./backend` / `dotnet publish ./backend/Hekutenantcoreapp.Api`; also runs on `pull_request` (build + test only; deploy is push-to-main). `angular.json` drops its `outputPath` override so `npm run build` writes to `frontend/dist/frontend/browser`. Family-wide standardization — see ludemia / gestamind / hekucoreapp.
- [x] **Validate email during registration (2026-09-09)** — added an admin-configurable **"require email confirmation"** toggle plus the verify flow, ported from the hekucoreapp core. New generic `AppSettings` singleton infrastructure alongside the existing `MultiTenantSettings` one: `app_settings` entity (Id=1), `AppSettingsPermission {Read,Update}` in `PermissionCatalog`, service/repository/DTOs, `AppSettingsConfiguration` + `AppSettingsSeeder`, `GET/PUT api/admin/app-settings`, `admin/settings` Angular page (in the System → Platform nav submenu). Off by default (accounts created `EmailConfirmed = true`, no behaviour change); when on, `AuthService.RegisterAsync` creates the user unconfirmed (still creating the tenant membership), emails a `/confirm-email?userId=&token=` link (`EmailTemplates.ConfirmEmail`, `App:FrontendBaseUrl` config with request-origin fallback), issues no token, and `LoginAsync` hard-blocks unconfirmed users after the password check. New `POST api/auth/confirm-email` + `POST api/auth/resend-confirmation` (always 200). Google OAuth unaffected and now also confirms a pre-existing unconfirmed account on link; admin-provisioned users created confirmed. Migration `AddAppSettings` creates the table and grandfathers every existing account (`UPDATE "AspNetUsers" SET "EmailConfirmed" = true`). Frontend: `confirm-email` dual-mode page, `register` redirect, `login` resend link, i18n en+es. **Verified:** `dotnet build`, `dotnet test` (54/54), `ng build` all clean; migration created + `has-pending-model-changes` reports none; not yet applied to Supabase.
- [x] **Moved `BootstrapAdminEmail` out of `appsettings.json` into user-secrets** — the key in `backend/Hekutenantcoreapp.Api/appsettings.json` held a real personal email in a tracked, non-gitignored file. Set it via `dotnet user-secrets set "BootstrapAdminEmail" "<email>" --project backend/Hekutenantcoreapp.Api` and blanked the default to `""` in `appsettings.json` (same pattern already used for `Jwt:Key`, `SmtpUser`, `SmtpPassword`). `Program.cs` already skips break-glass recovery when the value is empty (`string.IsNullOrEmpty(bootstrapEmail) ? null : ...`), so a blank default is safe. The address stays in git history — this only stops further exposure. Applied to gestamind in the same pass; ludemia and hekucoreapp not touched this pass.
- [x] **Renamed `backend/Hekutenantcoreapp.Infrastructure/Respositories/` → `Repositories/`** — the folder name was misspelled while the namespace declared inside the files already read `Hekutenantcoreapp.Infrastructure.Repositories`. Pure `git mv` folder rename, no code change (SDK-style csproj globs `.cs`); backend build clean afterward. Applied to gestamind in the same pass; ludemia and hekucoreapp not touched this pass.
- [x] **Fixed submenu misalignment in nested nav menus** — the System menu's Platform/Identity submenus (and the user menu's Identity & Organization submenu) popped up well above the row that opened them, with a gap the mouse would fall out of before reaching them. Root cause: Material's `mat-menu` nudges a submenu vertically by its parent menu's first-item `offsetTop`, as a correction for the parent panel's own inner padding — a heuristic that breaks once the parent has non-item content above its first item, which this nav's `.user-info` header block always does. Added `shared/submenu-align.directive.ts` (`appSubmenuAlign`, applied to each submenu-triggering button) which cancels just that erroneous vertical offset after the submenu opens and caps its height with scroll as a fallback for tight viewports. Ported from the same fix in ludemia, where it was verified in the browser via a scripted Playwright check (measured trigger vs. submenu panel coordinates before/after).
- [x] **Default tenant login** — admin-configurable setting so a user with a personal default tenant skips the post-login tenant-picker and logs straight into that tenant; admins can set/change a user's default tenant.
- [x] **Disable multitenant functionality** — admin-configurable setting to hide tenant-selection UI (registration, nav-bar) app-wide and route login/registration straight to a single default tenant, without locking out users who already belong to other tenants.
