// Single source of truth for the app's active languages — drives Transloco's availableLangs
// (app.config.ts) and the translatable-field component's per-language inputs. Add a language
// here and both follow automatically.
export const AVAILABLE_LANGUAGES = ['en', 'es'];
export const BASE_LANGUAGE = 'en';
