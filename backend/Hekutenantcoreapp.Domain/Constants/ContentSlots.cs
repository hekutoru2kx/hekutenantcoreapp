namespace Hekutenantcoreapp.Domain.Constants;

// Named singleton slots on ContentItem (see ContentItem.Slot). Add one here per feature that
// needs an "at most one" content item rather than an ordered list.
public static class ContentSlots
{
    public const string ProfilePicture = "ProfilePicture";
}
